import { WhatsappConfigClient } from '@/components/admin/WhatsappConfigClient';

export const metadata = {
  title: 'WhatsApp Automático | Admin',
};

export default function WhatsappPage() {
  return <WhatsappConfigClient />;
}
