import { MesasClient } from '@/components/admin/pedidos/MesasClient';

export const metadata = {
  title: 'Gestão de Mesas | Admin',
};

export default function MesasPage() {
  return <MesasClient />;
}
