import { ImportacoesClient } from '@/components/admin/financeiro/ImportacoesClient';

export const metadata = { title: 'Importações CSV | Admin' };

export default function ImportacoesPage() {
  return <ImportacoesClient />;
}
